# Copy this file to `.env` and set local values.
# The app does not auto-load `.env`; load vars via shell/IDE tooling.

# App environment selector used by EnvConfig (dev|prod)
APP_ENV=dev

# Cognito app client secret (do NOT commit a real value)
COGNITO_CLIENT_SECRET=changeme

# Database settings (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cf_test_db
DB_USER=postgres
DB_PASS=changeme
